# Verification-only defaults. These values are merged into schema.yaml by the
# Marketplace deployer and are not exposed as customer configuration.
name: TMR World Engine verification
description: Verification defaults for the gRPC/mTLS smoke test.
version: 0.5.17
application_api_version: v1beta1
properties:
  test.enabled:
    type: boolean
    default: true
  test.image:
    type: string
    default: asia-southeast1-docker.pkg.dev/tmr-we-sdk-pilot/tmrwe/tester:0.5.11
